/*index starts From 1 and it keeps on increasing for each value of the index we check its previsous elements
if the previous element is greater than the index element we then move the previous element to the index and
we then check the element before the previous element if that element is greater than the index then again
the same process is repeated!else the element is placed in its appropriate plce!*/

#include <stdio.h>

int main () {
  int Array[10]={9,7,85,64,31,28,73,91,2,5};
  int i,j,k,Key,Temp;
  //MAin Insertion Sort Code Here!//

  for(i=0;i<10;i++){}
}
